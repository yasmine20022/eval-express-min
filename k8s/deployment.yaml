apiVersion: apps/v1
kind: Deployment
metadata:
  name: eval-express-min
  labels:
    app.kubernetes.io/name: eval-express-min
    app.kubernetes.io/instance: eval-express-min
    app.kubernetes.io/part-of: eval-express-min
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eval-express-min
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: eval-express-min
        app.kubernetes.io/name: eval-express-min
        app.kubernetes.io/instance: eval-express-min
        app.kubernetes.io/part-of: eval-express-min
    spec:
      containers:
      - name: eval-express-min
        image: eval-express-min:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          failureThreshold: 30
          periodSeconds: 10
